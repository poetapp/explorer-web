import React from 'react'

import { Main } from 'components/templates/Main'

import classNames from './NewClaim.scss'

export const NewClaim = () => {
  return (
    <Main>
      <section className={classNames.newClaim}>...</section>
    </Main>
  )
}
